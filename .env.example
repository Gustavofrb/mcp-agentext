# Diretório onde o servidor pode ler/escrever arquivos (sandbox)
FILE_MANAGER_SANDBOX=./sandbox

# Host e porta do servidor
FILE_MANAGER_HOST=0.0.0.0
FILE_MANAGER_PORT=8000

# API key para autenticação (deixe vazio para desabilitar)
FILE_MANAGER_API_KEY=
